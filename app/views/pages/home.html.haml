.container
  - title 'FAIMS Server'
  - if !user_signed_in?
    = simple_form_for("user", :url => user_session_path) do |f|
      = render(:partial => 'devise/sessions/form', :locals => { :f => f })
  - else
    = link_to "Show Projects", projects_path, :class => "btn"
