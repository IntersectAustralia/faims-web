- if !group[key].nil?
  - if (checked)
    %input{:type => 'radio', :checked => 'checked'}
    .attrib
      = render :partial => 'compare_reln_row_content', :locals => { group: group, key: key, value: value }
      %span.attribute-info
        = attributeInfo[key]
  - else
    %input{:type => 'radio'}
    .attrib
      = render :partial => 'compare_reln_row_content', :locals => { group: group, key: key, value: value }
      %span.attribute-info
        = attributeInfo[key]